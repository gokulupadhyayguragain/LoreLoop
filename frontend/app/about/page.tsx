import { PageIntro } from "@/components/PageIntro";

export default function AboutPage() {
  return <><div className="container"><PageIntro eyebrow="About LoreLoop" title="A story with its own morning routine." copy="Most generative tools wait for a prompt. LoreLoop starts with a world, remembers it, and gives that world another day to change." /></div><section className="section"><div className="container"><div className="memory-panel"><div className="memory-card"><div className="eyebrow">The idea</div><h3>Autonomous creativity needs continuity.</h3><p style={{ color: "var(--paper-muted)", lineHeight: 1.75 }}>LoreLoop is a weekend challenge project built around one question: what happens when an AI creative system is asked to keep a fictional universe alive rather than produce disconnected one-off outputs?</p></div><div className="memory-card"><div className="eyebrow">The constraint</div><h3>Small systems can still feel alive.</h3><p style={{ color: "var(--paper-muted)", lineHeight: 1.75 }}>The product deliberately avoids accounts, feeds, comments, and manual generation as its center. The interesting part is the scheduled loop: memory, choice, validation, publication.</p></div></div></div></section><section className="section"><div className="container"><div className="eyebrow">What powers it</div><h2 className="section-title">AWS as a creative substrate.</h2><div style={{ height: 22 }} /><p className="lede">EventBridge Scheduler provides the heartbeat. Lambda coordinates the work. Bedrock Nova writes and checks lore. DynamoDB remembers the canon. Nova Canvas and S3 hold the visual layer. CloudWatch makes the autonomous behavior inspectable. API Gateway keeps the frontend read-only.</p></div></section></>;
}

